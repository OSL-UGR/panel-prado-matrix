Este archivo ubicado en `api/routes.py` es uno de los principales de nuestra aplicación. Aquí se definen todas las URLS o endpoints a las que el frontend puede llamar para realizar peticiones, pidiendo o enviando datos. 

Su función principal  es recibir las peticiones HTTP, validar los parámetros de entrada y derivar el trabajo a los "servicios" para finalmente devolver una respuesta a la interfaz web. Las funciones que importamos de los servicios y que utilizamos a lo largo de nuestras rutas son:

``` python
# Importamos los dos servicios
from app.services.matrix_api import(
	obtener_info_sala,
	obtener_perfil_usuario,
	registrar_usuarios_matrix,
	crear_espacio_asignatura,
	insertar_alumnos_sala,
	arreglar_jerarquia,
	crear_nodo,
	editar_nodo,
	eliminar_nodo,
	accionar_celda_horario
)

from app.services.prado_api import(
	obtener_alumnos_prado_service,
	obtener_total_usuarios_usuario,
	obtener_total_asignaturas_usuario,
	obtener_asignaturas_usuario
)
```

Si tenemos desplegado en local nuestra aplicación, FastAPI nos incluye una interfaz de pruebas con la que poder consultar y ejecutar nuestras rutas definidas en este archivo. Podemos visitar esto en: http://localhost:8000/docs

A su vez también utilizamos este archivo para definir todos aquellos nodos de modelos de datos que se necesitan para insertar/actualizar datos a través de los distintos formularios de nuestra aplicación. Estos nodos son: 

``` python
# ==========================================
# MODELOS DE DATOS
# ==========================================
class CrearNodoRequest(BaseModel):
	nombre: str
	descripcion: str
	tipo: str
	id_padre: str
	auto_añadir: bool = False

class EditarNodoRequest(BaseModel):
	nombre: str
	descripcion: str
	tipo: str

class ActualizarCronogramaRequest(BaseModel):
	matriz: List[List[int]] # Esperamos una matriz de 7*24

class CrearMensajeProgRequest(BaseModel):
	sala_id: int
	contenido: str
	fecha_envio: datetime

class EditarMensajeProgRequest(BaseModel):
	contenido: str
	fecha_envio: datetime
```

A continuación mostramos el listado de todas las rutas configuradas en el sistema:
## Rutas de Matrix y Prado 

**GET `/matrix/salas/{room_id}`**

- Recibe el ID de una sala en matrix. Consulta directamente a la API de Synapse para devolver la información de la sala como su nombre, tipo, listado de miembros etc.

**GET `/matrix/usuarios/{user_id}/perfil`**

- Recibe el ID de un usuario en Matrix. Se comunica con el servidor de Matrix para devolver le nombre del usuario.

**GET `/prado/asignaturas/{asignatura_id}/alumnos`**

- Recibe el ID de una asignatura de Prado. Consulta el simulador de datos de Moodle/Prado para listar y devolver a todos los estudiantes matriculados de la asignatura.

**GET `/prado/usuarios/{user_id}/asignaturas`**

- Recibe el ID de un profesor de Prado. Consulta el simulador de prado para devolver todas las asignaturas que imparte ese profesor.
## Sincronización y Gestor de Salas

**POST `/prado/asignaturas/{asignatura_id}/sincronizar`**

- Recibe el ID de una asignatura de Prado. Se encarga de todo el proceso de sincronizar una asignatura de Prado con un espacio de Matrix. Crea el espacio con el profesor como administrador e inyecta a los alumnos como usuarios normales.

**GET `/prado/asignaturas/{asignatura_id}/salas`**

- Recibe el ID de una asignatura de Prado. Se encarga de leer nuestra base de datos PostgreSQL  y devuelve toda la jerarquía de salas asignada a una asignatura (pertenecientes a su espacio de Matrix).

**POST `/prado/asignaturas/{asignatura_id}/salas`**

- Recibe el ID de una asignatura de Prado.  Recibe los datos del cuestionario del front (nombre, descripción, tipo, id_padre,  autoañadir). Crea la nueva sala en Matrix, auto-matricula a los alumnos si se ha marcado la casilla y registra el nuevo nodo en la jerarquía de salas.

**PUT `/prado/asignaturas/{asignatura_id}/salas/{room_id}`**

- Recibe el ID de una asignatura en Prado y el ID de una sala en Matrix. Aplica los cambios de nombre, descripción y tipo de sala obtenidos del formulario de edición para editar los parámetros de una sala.

**DELETE `/prado/asignaturas/{asignatura_id}/salas/{room_id}`**

- Recibe el ID de una asignatura en Prado y el ID de una sala en Matrix. Se encarga de eliminar una sala de la jerarquía de salas de una asignatura.
## Estadísticas (Panel de Inicio)

**GET `/inicio/estadisticas`**

- No recibe parámetros de entrada, Devuelve contadores de estadísticas como el total de alumnos, asignaturas y salas extraídos de Prado y de la bd local.
## Cronograma de Salas (La Berja)

**GET `/prado/asignaturas/{asignatura_id}/salas/{room_id}/cronograma`**

- Recibe el ID de una asignatura en Prado y el ID de una sala en Matrix. Se encarga de consultar la base de datos y devolver la matriz de horarios actual para que el front pueda mostrarla.

**PUT `/prado/asignaturas/{asignatura_id}/salas/{room_id}/cronograma`**

- Recibe el ID de una asignatura en Prado y el ID de una sala en Matrix. Se encarga de sobrescribir con una nueva matriz de horarios la actual de la base de datos, enviando peticiones al Matrix para abrir o cerrar la sala seleccionada.
## Programador de Avisos

**GET `/prado/mensajes`**

- No recibe parámetros de entrada. Se encarga de obtener los mensajes programados en estado de "pendiente" de la base de datos ordenados del más cercano al más lejano.

**POST `/prado/mensajes`**

- No recibe parámetros de entrada. Se encarga de insertar un mensaje programado a través del formulario con la ID de la sala destino, la fecha de envío y el contenido. Guarda este mensaje en la base de datos PorstgreSQL a la espera de que el cron lo detecte y lo envíe cuando llegue el momento.

**PUT `/prado/mensajes/{mensaje_id}`**

- Recibe el ID de un mensaje mensaje programado. A través del formulario de edición de mensajes actualiza su texto, fecha o hora en la base de datos local.


**DELETE `/prado/mensajes/{mensaje_id}`**

-  Recibe el ID de un mensaje mensaje programado. Se encarga de eliminar el envío de un mensaje programado borrándolo de la base de datos.
## Registro de Logs

**GET `/sistema/logs`**

- No recibe parámetros de entrada.
No pide parámetros. Consulta la tabla de registros y devuelve el historial completo de acciones de auditoría del sistema ordenado cronológicamente para mostrarlo en la consola de la aplicación.